package com.dagurasu.libraryapi.config;

import org.springframework.context.annotation.Configuration;

@Configuration
//@EnableSwagger2
public class SwaggerConfig {

	/*
	 * @Bean public Docket docket() { return new Docket(DocumentationType.SWAGGER_2)
	 * .select() .apis(RequestHandlerSelectors.basePackage(
	 * "com.dagurasu.libraryapi.api.resource")) .paths(PathSelectors.any()) .build()
	 * .apiInfo(apiInfo()); }
	 * 
	 * private ApiInfo apiInfo() { return new ApiInfoBuilder() .title("Library API")
	 * .description("API do Projeto de controle de aluguel de livros")
	 * .version("1.0") .contact(contact()) .build(); }
	 * 
	 * private Contact contact() { return new Contact("Douglas Souza",
	 * "https://github.com/Dagurasu56", "contini.ds@gmail.com"); }
	 */
}
